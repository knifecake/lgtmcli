# AGENTS

- Use the `Makefile` as the command reference for local workflows (`make build`, `make`, `make install`, `make lint`, `make test`).
- Most user-visible changes should add a short bullet to `CHANGELOG.md`.
