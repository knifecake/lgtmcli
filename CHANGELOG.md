# Changelog

## Unreleased

- Add low-friction auth login flow (`auth login`) with saved profile support (XDG config path), extensible profiles file schema, and credential precedence: flags > env vars > profile
- Add Linux arm64 release artifact and installer support
- Drop macOS x86_64 release artifacts (Apple Silicon only)

## v0.1.0 - 2026-03-26

- Initial version
