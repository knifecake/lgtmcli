# Rust build artifacts
/target

# Cargo lockfile
# Keep Cargo.lock for binaries/apps (this project is a binary), so don't ignore it.

# Local environment secrets/config
.env
.env.*
!.env.example
*.envrc
!.envrc.example

# local notes / scratch
WIP.md

# macOS
.DS_Store
